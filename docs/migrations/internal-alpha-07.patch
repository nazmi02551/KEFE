--- a/apps/mobile/lib/features/weigh/presentation/weigh_hub_screen.dart
+++ b/apps/mobile/lib/features/weigh/presentation/weigh_hub_screen.dart
@@ -3,6 +3,7 @@
 import 'package:go_router/go_router.dart';
 
 import '../../../core/design/kefe_theme.dart';
+import '../../../core/localization/internal_alpha_strings.dart';
 import '../../../core/localization/kefe_strings.dart';
 import '../../decision/domain/decision_models.dart';
 import '../../explore/application/explore_controller.dart';
@@ -245,24 +246,3 @@
     ),
   );
 }
-
-extension WeighHubStrings on KefeStrings {
-  bool get _weighHubIsTurkish => locale.languageCode == 'tr';
-
-  String get weighHubEyebrow => _weighHubIsTurkish ? 'TARTIM' : 'WEIGH';
-  String get weighHubTitle => _weighHubIsTurkish
-      ? 'Sıradaki kararını seç.'
-      : 'Choose your next decision.';
-  String get weighHubSubtitle => _weighHubIsTurkish
-      ? 'Önce kendi kararını ver; topluluk sonucu yalnız Commit sonrasında açılır.'
-      : 'Decide first; collective results unlock only after Commit.';
-  String get weighHubRecommended =>
-      _weighHubIsTurkish ? 'ÖNERİLEN TARTIM' : 'RECOMMENDED WEIGH';
-  String get weighHubStart =>
-      _weighHubIsTurkish ? 'Tartıma başla' : 'Start weighing';
-  String get weighHubMore =>
-      _weighHubIsTurkish ? 'Diğer tartımlar' : 'More Cases';
-  String get weighHubEmpty => _weighHubIsTurkish
-      ? 'Şu anda tartıma açık bir vaka yok.'
-      : 'No Cases are currently open for weighing.';
-}
--- a/apps/mobile/lib/features/sharing/presentation/public_share_screen.dart
+++ b/apps/mobile/lib/features/sharing/presentation/public_share_screen.dart
@@ -2,6 +2,8 @@
 import 'package:flutter_riverpod/flutter_riverpod.dart';
 import 'package:go_router/go_router.dart';
 
+import '../../../core/localization/internal_alpha_strings.dart';
+import '../../../core/localization/kefe_strings.dart';
 import '../../decision/data/decision_repository.dart';
 import '../../decision/data/http_decision_repository.dart';
 import '../application/share_controller.dart';
@@ -56,7 +58,7 @@
 
   @override
   Widget build(BuildContext context) {
-    final tr = Localizations.localeOf(context).languageCode == 'tr';
+    final strings = KefeStrings.of(context);
     return Scaffold(
       appBar: AppBar(title: const Text('KEFE')),
       body: SafeArea(
@@ -67,20 +69,18 @@
               const Center(child: CircularProgressIndicator())
             else if (_share == null) ...[
               Text(
-                tr
-                    ? 'Bu paylaşım artık kullanılamıyor.'
-                    : 'This share is no longer available.',
+                strings.publicShareUnavailable,
                 key: const ValueKey('public-share-error'),
               ),
               if (_errorCode != null) Text(_errorCode!),
               const SizedBox(height: 12),
               OutlinedButton(
                 onPressed: _load,
-                child: Text(tr ? 'Tekrar dene' : 'Try again'),
+                child: Text(strings.publicShareRetry),
               ),
             ] else ...[
               Text(
-                tr ? 'Bir KEFE vakası paylaşıldı' : 'A KEFE case was shared',
+                strings.publicShareEyebrow,
                 style: Theme.of(context).textTheme.labelLarge,
               ),
               const SizedBox(height: 8),
@@ -94,19 +94,17 @@
               const SizedBox(height: 10),
               Text(_share!.summary),
               const SizedBox(height: 12),
-              Chip(label: Text(_share!.primaryDomain)),
+              Chip(label: Text(strings.domainName(_share!.primaryDomain))),
               const SizedBox(height: 18),
               Text(
-                tr
-                    ? 'Paylaşan kişinin kararı burada gösterilmez. Önce aynı vakayı kendin tart ve kararını sabitle; topluluk sonucu ancak kendi Commit’inden sonra açılır.'
-                    : 'The sender’s decision is never shown here. Weigh the same case and Commit first; collective results unlock only after your own Commit.',
+                strings.publicShareBlindFirst,
               ),
               const SizedBox(height: 16),
               FilledButton.icon(
                 key: const ValueKey('public-share-weigh'),
                 onPressed: () => context.go('/case/${_share!.caseId}'),
                 icon: const Icon(Icons.balance_rounded),
-                label: Text(tr ? 'Ben de tartayım' : 'Weigh it myself'),
+                label: Text(strings.publicShareWeigh),
               ),
             ],
           ],
