# Roadmap registry

The active GitHub delivery mirror is `capability-portfolio.v1.tsv`. Read `CAPABILITY_PORTFOLIO.md` for authority, lifecycle and continuation rules. The full v3.5 WORKING JSON/YAML/CSV registry remains inside the documented Drive WORKING package.
