import { TrustIntegrityWorkspace } from "../../src/components/trust-integrity-workspace";

export const metadata = {
  title: "Trust & Integrity Shield | KEFE Admin Studio",
  description: "Bot, anomaly and coordinated manipulation detection workspace (CAP-073)",
};

export default function TrustIntegrityPage() {
  return <TrustIntegrityWorkspace />;
}
