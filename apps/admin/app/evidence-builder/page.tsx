import { EvidenceBuilderWorkspace } from '../../src/components/evidence-builder-workspace';

export const metadata = {
  title: 'Kanıt İnşa Edici | KEFE Admin Studio',
  description: 'CAP-098 Kanıt ve kaynak bağlama masası.',
};

export default function EvidenceBuilderPage() {
  return <EvidenceBuilderWorkspace />;
}
