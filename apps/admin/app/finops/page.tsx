import { FinOpsWorkspace } from "../../src/components/finops-workspace";

export const metadata = {
  title: "FinOps Intelligence | KEFE Admin Studio",
  description: "Unit economics, Cost per Weigh (CPW), provider breakdown and scale simulation (CAP-124)",
};

export default function FinOpsPage() {
  return <FinOpsWorkspace />;
}
