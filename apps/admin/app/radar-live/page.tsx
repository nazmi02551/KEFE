import { RadarLiveWorkspace } from "../../src/components/radar-live-workspace";

export const metadata = {
  title: "Live Radar & Context Drift | KEFE Admin Studio",
  description: "Live deliberation momentum, demographic shift vectors and context drift notice publishing (CAP-076)",
};

export default function RadarLivePage() {
  return <RadarLiveWorkspace />;
}
