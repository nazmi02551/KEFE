name: Slice7 Format Fix (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'Temporary Slice 7 formatter is disabled; source formatting is already canonical.'
